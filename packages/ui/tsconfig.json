{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "lib": ["ES2023", "DOM", "DOM.Iterable"],
    "jsx": "react-jsx",
    // For the image and SVG imports the crests and position icons are.
    "types": ["vite/client"]
  },
  "include": ["src/**/*"]
}
